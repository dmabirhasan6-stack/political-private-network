// Reuse the verified client dashboard implementation.
// This route previously contained an untyped duplicate dashboard that failed strict TypeScript checks.
export { default } from './real-dashboard';
